{
  "name": "Google-AI-Search-MCP",
  "description": "A minimal Node.js server that exposes a /search endpoint. It uses the Gemini API with Google Search grounding to answer queries.",
  "author": "",
  "license": "ISC",
  "sdk": {
    "name": "node",
    "version": ">=18.0.0"
  },
  "tools": [
    {
      "name": "search",
      "description": "Get answers from Google Search using the Gemini API.",
      "entrypoint": "index.js",
      "command": "node index.js",
      "requires_auth": true,
      "auth_instructions": "Ensure the GEMINI_API_KEY environment variable is set."
    }
  ]
}
